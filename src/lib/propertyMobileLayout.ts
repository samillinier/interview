/**
 * Extra left padding on small screens so titles and content clear the fixed
 * hamburger in PropertyMobileMenu. Includes safe-area for notched devices.
 */
export const propertyMobileSafeLeftPad =
  'max-lg:pl-[calc(env(safe-area-inset-left,0px)+4rem)]'
